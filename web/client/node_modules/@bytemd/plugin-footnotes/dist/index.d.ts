import type { BytemdPlugin } from 'bytemd';

declare function footnotes(options?: Options): BytemdPlugin;
export default footnotes;

/**
 * Configuration.
 */
declare type Options = {
    /**
     * Whether to support `^[inline notes]`.
     */
    inlineNotes?: boolean | undefined
}

export { }
